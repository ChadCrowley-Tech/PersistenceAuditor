<Application x:Class="PersistenceAuditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <SolidColorBrush x:Key="DeepCanvas" Color="#05050A" />
        <SolidColorBrush x:Key="SidebarSurface" Color="#0A0A12" />
        <SolidColorBrush x:Key="CardSurface" Color="#11101A" />

        <SolidColorBrush x:Key="ColorNetwork" Color="#00FFFF" />
        <SolidColorBrush x:Key="ColorExecution" Color="#FF6D00" />
        <SolidColorBrush x:Key="ColorIdentity" Color="#FF2A85" />
        <SolidColorBrush x:Key="ColorFuture" Color="#9D4EDD" />
        <SolidColorBrush x:Key="TextPrimary" Color="#F4F7FA" />
        <SolidColorBrush x:Key="TextSecondary" Color="#8B9BB4" />

        <Style TargetType="TextBlock" x:Key="GlowingTitle">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00E5FF" BlurRadius="12" ShadowDepth="0" Opacity="0.7"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="UniversalCardText">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="1.5" Opacity="0.9"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Border" x:Key="CompactCard">
            <Setter Property="Background" Value="{StaticResource CardSurface}" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="BorderBrush" Value="#2D2844" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Opacity="0.3" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="CyberButton">
            <Setter Property="Background" Value="#00FFFF"/>
            <Setter Property="Foreground" Value="#050B14"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="8" Padding="20,10">
                            <Border.Effect>
                                <DropShadowEffect Color="#00FFFF" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#050B14" />
                                <Setter Property="Foreground" Value="#00FFFF" />

                                <Setter TargetName="BtnBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FFFF" BlurRadius="25" ShadowDepth="0" Opacity="1.0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
